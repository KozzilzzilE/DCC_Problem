# Mission 1 (신고자 성별 분류) 실행 환경 — 이 폴더 단독 제출 기준
#
# torch / torchvision 은 채점 머신의 GPU 에 맞는 CUDA 빌드를 받아야 하므로 아래에서
# 버전을 고정하지 않는다 (cu118 로 고정하면 RTX 50 시리즈에서 설치조차 되지 않는다).
# 학습·검증에 쓴 조합: torch 2.13.0+cu130, torchvision 0.28.0+cu130 (Python 3.14).
#
# 설치 (이 폴더에서):
#   python -m venv venv
#   venv\Scripts\python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#     (RTX 30/40 이면 cu121, CPU 전용이면 --index-url 생략)
#   venv\Scripts\python -m pip install -r requirements.txt
#
# torchaudio 는 쓰지 않는다 (log-Mel 을 torch.stft 로 직접 구현). librosa 는 테스트의
# 수치 대조에만 쓰인다. 사전학습 모델의 config/가중치는 ckpt/*.pt 에 동봉돼 있어
# 추론 시 인터넷 접속이 없다.

# --- GPU 스택 (위 index-url 로 별도 설치) ---
# torch==2.13.0+cu130
# torchvision==0.28.0+cu130

# --- 핵심 ---
transformers==5.15.0
accelerate==1.14.0
numpy==2.4.6
scipy==1.18.0
pandas==3.0.5
scikit-learn==1.9.0

# --- 오디오 I/O ---
soundfile==0.14.0

# --- 리포트 / 노트북 ---
matplotlib==3.11.1
tqdm==4.70.0
nbformat==5.11.1
nbconvert==7.17.1
ipykernel==7.3.0

# --- 테스트 (librosa 는 피처 수치 대조용) ---
pytest==9.1.1
librosa==1.0.0
